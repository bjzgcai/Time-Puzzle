{
  "name": "people",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --port=8000 --watch=. --ignore=node_modules --no-browser",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
